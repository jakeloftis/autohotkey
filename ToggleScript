#MaxThreadsPerHotkey 2
F6:: ; Assign key here to toggle script on and off, F6 is currently assigned
Toggle := !Toggle
loop
{
    If not Toggle
  {
        break ; breaks the script when key at top of page is pressed, can enter additional scripts above "break" to reset keys in down position with "send {w up}" or the likes
  }
	      DELETE-THIS-AND-TYPE-SCRIPT ; script goes here, type what to toggle on
}
return
